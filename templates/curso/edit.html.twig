{% extends 'base.html.twig' %}

{% block title %}Edit Curso{% endblock %}

{% block body %}
    <h1>Edit Curso</h1>
<section id = "edit">
    <div id = "form">
    {{ include('curso/_form.html.twig', {'button_label': 'Update'}) }}
    </div>

    <button>
	<a href="{{ path('curso_list') }}">Volver ↩</a>
	</button>
</section>    

    {{ include('curso/_delete_form.html.twig') }}
{% endblock %}
